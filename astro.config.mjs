// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// baseName: com-vc, css-tws, dev-frontend
export default defineConfig({
  site: "https://tdtc-hrb.github.io",
  base: "baseName",
  trailingSlash: 'ignore',
});
